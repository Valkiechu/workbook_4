let person = {
    name: "What it to you",
    address: "why you need to care street",
    city: "Pittsburgh",
    state: "PA",
    zip: 15241,
};
function printContact (person){
    console.log(person.name);
    console.log(person.address);
    console.log(person.city);
    console.log(person.state);
    console.log(person.zip);
}
printContact(person);

// formating neeed to be above to function

